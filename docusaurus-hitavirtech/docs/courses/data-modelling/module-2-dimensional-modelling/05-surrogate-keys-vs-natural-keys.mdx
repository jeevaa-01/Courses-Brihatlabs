---
title: 'Surrogate Keys vs Natural Keys'
sidebar_position: 5
description: 'Why data warehouses use system-generated integer surrogate keys instead of business (natural) keys — and keep both.'
---

<DurationBadge minutes={5} />

<LearningObjectives>

- Distinguish surrogate keys from natural (business) keys
- Explain why warehouses standardize on integer surrogate keys
- Apply the industry pattern: surrogate as PK, natural key kept for traceability

</LearningObjectives>

### What Are They?

![Surrogate vs Natural Keys](/img/courses/diagrams/surrogate-vs-natural.svg)

### Why Surrogate Keys in Data Warehouses?

```sql
-- Natural key problems:
-- 1. Business keys can CHANGE (company merges, system migration)
-- 2. Composite natural keys make JOINs slow
-- 3. Different source systems use different IDs for same entity

-- Surrogate key solution:
CREATE TABLE dim_customer (
    customer_key    INT AUTO_INCREMENT PRIMARY KEY,  -- Surrogate (DW key)
    customer_id     VARCHAR(20) NOT NULL,             -- Natural (source key)
    first_name      VARCHAR(50),
    ...
);
```

### Performance Impact

```
Natural key JOIN (slow — string comparison):
  ON fact.customer_id = dim.customer_id     -- VARCHAR comparison

Surrogate key JOIN (fast — integer comparison):
  ON fact.customer_key = dim.customer_key   -- INT comparison
```

Integer JOINs are **3-5x faster** than string JOINs on large datasets.

:::tip[HitaVir Tech Best Practice]
"Always use surrogate keys (integers) in your data warehouse dimension tables. Keep the natural key as a separate column for traceability. This is a universal industry standard."
:::

<KeyTakeaways>

- A surrogate key is a meaningless auto-generated integer; a natural key is a real business identifier
- Business keys can change and differ across source systems — surrogates insulate the warehouse from that
- Integer JOINs run 3-5x faster than VARCHAR JOINs on large datasets
- Standard pattern: surrogate key as the PK/FK, natural key kept as a column for source traceability

</KeyTakeaways>

<Quiz
  questions={[
    {
      question: 'Which of these is a surrogate key?',
      options: [
        'email_address',
        'An auto-increment integer customer_key generated by the warehouse',
        'national_id_number',
        'customer_id from the CRM source system',
      ],
      answer: 1,
      explanation:
        'Surrogate keys are system-generated and carry no business meaning; the others are natural (business) identifiers.',
    },
    {
      question: 'Why do warehouses avoid natural keys as dimension primary keys?',
      options: [
        'Natural keys cannot be indexed',
        'SQL forbids VARCHAR primary keys',
        'Business keys can change and differ across source systems, and string JOINs are slower',
        'Natural keys take zero storage',
      ],
      answer: 2,
      explanation:
        'Mergers, migrations, and multi-source integration break natural keys, and VARCHAR JOINs are 3-5x slower than INT JOINs.',
    },
    {
      question: 'What should happen to the natural key when you adopt surrogate keys?',
      options: [
        'Drop it from the warehouse entirely',
        'Concatenate it into the surrogate key',
        'Use it as the fact table primary key instead',
        'Keep it as a regular column in the dimension for traceability',
      ],
      answer: 3,
      explanation:
        'The natural key stays as a column so every warehouse row can be traced back to its source system record.',
    },
  ]}
/>

<LessonComplete />
