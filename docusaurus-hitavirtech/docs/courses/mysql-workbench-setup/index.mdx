---
title: About this course
sidebar_position: 0
slug: /courses/mysql-workbench-setup
description: Install MySQL Server and MySQL Workbench on Windows from scratch, connect with the root account, run your first SQL queries, and import a real database.
---

# MySQL Workbench Setup and Installation Guide

**Instructor:** HitaVir Tech · **Level:** Beginner · **Estimated total:** ~1 hour · **4 modules · 11 lessons**

Welcome to the **MySQL Workbench Setup and Installation Guide** by **HitaVir Tech**!
This course walks you through installing MySQL Server and MySQL Workbench on
**Windows** from scratch. By the end, you will have a fully working database
environment and will have run your first SQL queries.

<LearningObjectives>

- Install MySQL Server, Workbench, and Shell with the official MySQL Installer
- Configure the server (port 3306, root password, Windows service)
- Connect with Workbench and run your first CREATE / INSERT / SELECT / UPDATE queries
- Import an existing database from a `.sql` file and troubleshoot common issues

</LearningObjectives>

## What is MySQL?

**MySQL** is the world's most popular open-source **relational database**. It
stores data in organized tables — rows and columns — just like a spreadsheet,
but much more powerful.

**Real-world analogy:** Think of MySQL as a giant, super-fast filing cabinet.
Each drawer (database) contains folders (tables), and each folder has organized
records (rows).

## What is MySQL Workbench?

**MySQL Workbench** is the official **graphical tool** for MySQL. It lets you:

- Write and run SQL queries
- Design database schemas visually
- Manage users and permissions
- Import and export data
- Monitor server performance

**Real-world analogy:** If MySQL Server is the engine of a car, MySQL Workbench
is the dashboard — it gives you a visual way to control everything.

## Why MySQL for Data Engineering?

- Used by **Facebook, Twitter, YouTube, Netflix** and thousands of companies
- Powers **web applications, analytics dashboards, and data pipelines**
- **Free and open source** — no license fees
- SQL skills are required in **95% of data engineering job postings**
- Works with Python (SQLAlchemy, pandas), Spark, Airflow, and all major tools

## Prerequisites

- A computer running **Windows 10 or 11** (64-bit) with administrator access
- At least **2 GB** free disk space
- Stable internet connection (the installer is approximately 450 MB)
- Zero database or SQL experience needed — we start from the download page

## Syllabus

| Module | Lessons | Time |
|--------|---------|------|
| **1 · Get Ready & Download** | Prerequisites · Download MySQL Installer | ~7 min |
| **2 · Install & Configure MySQL Server** | Install MySQL Server · Configure MySQL Server | ~18 min |
| **3 · Connect & Query with Workbench** | Connect using Workbench · Run your first SQL queries · Import a database file | ~23 min |
| **4 · Troubleshoot & Wrap Up** | Troubleshooting · Summary & next steps · Final quiz · Congratulations | ~14 min |

:::tip[How to take this course]
Work through the lessons **in order** — configuration depends on the install,
and the query lessons assume a working connection. Each lesson ends with a
short knowledge check and a **Mark lesson as complete** button so you can track
where you left off. Keep your **root password** written down — you will need it
from Module 3 onward.
:::

Ready? Start with [Prerequisites](/courses/mysql-workbench-setup/module-1-get-ready/prerequisites).
